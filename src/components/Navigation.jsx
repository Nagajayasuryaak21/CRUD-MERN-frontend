
const Navigate = (props) => {
//   const navigate = useNavigate();
  return (
    <div>
      <div onClick={props.handleHome} className="Nav">
        <h1>STUDENTS REPORT</h1>
      </div>
    </div>
  );
};

export default Navigate;
